{
    "$schema": "../../../../query.schema.json",
    "description": "_id is the one field an inclusion projection returns without being asked, so {\"_id\": 0} is how you drop it. Nothing then identifies the documents, so expected.json has no ids to check and the documents themselves carry the assertion.",
    "dataset": "people",
    "filter": {"_id": {"$in": ["1", "3"]}},
    "projection": {"name": 1, "_id": 0}
}
